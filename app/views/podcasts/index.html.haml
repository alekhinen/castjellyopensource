%h1 CastJelly 
%h4 All Podcasts
%p This section contains all the content that you are currently able to subscribe to. <br> More content will be coming soon. 
.podcasts-container
  %h1 Podcasts
  - if current_user.admin
    = link_to 'New Podcast', new_podcast_path, :class => "btn btn-success", :id => "new-podcast-btn"
  .clear
  - @podcasts.each do |podcast|
    .podcast-container{:class => cycle('even', 'odd')}
      .podcast-image-container
        =image_tag podcast.image.url
      %a.title{:href => "#{podcast_path(podcast)}"} #{podcast.title}
      %p.small #{podcast.description}
      .clear
      - if current_user.admin
        = link_to 'Edit', edit_podcast_path(podcast), :class => "btn btn-success"
        = link_to 'Destroy', podcast, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger"





/   %tbody
/     - @podcasts.each do |podcast|
/       %tr
/         %td= podcast.title
/         %td= podcast.description
/         %td= podcast.link
/         %td= podcast.tags
/         %td= podcast.views
/         %td= podcast.created_at
/         %td= link_to 'Show', podcast
/         - if current_user.admin
/           %td= link_to 'Edit', edit_podcast_path(podcast)
/           %td= link_to 'Destroy', podcast, method: :delete, data: { confirm: 'Are you sure?' }
/ %br
/   - if current_user.admin
/     = link_to 'New Podcast', new_podcast_path